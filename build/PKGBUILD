# Maintainer: Egor Sozonov
# Contributor: Egor Sozonov

pkgbase=closha
pkgname=(closha)
pkgver=_
pkgrel=1
pkgdesc='Local file backup tool' 
arch=(x86_64)
url='https://github.com/EgorSozonov/closha'
license=(BSD-3-Clause)
depends=(coreutils) 
makedepends=(git)
source=(git+https://github.com/EgorSozonov/closha#tag=$pkgver)
options=(!debug !strip)
sha256sums=('SKIP')

package() {
   install -D closha.sh "$pkgdir/usr/bin/closha.sh"
}

